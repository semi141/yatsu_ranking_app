<div class="container mt-5">
  <%= form_with url: search_index_path, method: :get, local: true do |f| %>
    <div class="mb-2">
      <label class="form-label">タイトルで検索：</label>
      <%= f.text_field :keyword, placeholder: "動画タイトルを入力", class: "form-control" %>
    </div>

    <div class="mb-2">
      <label class="form-label">タグで絞り込み：</label>
      <%= f.select :tag, options_for_select(Video.tag_counts_on(:tags).pluck(:name)), { include_blank: "全て" }, { class: "form-select" } %>
    </div>

    <div class="mb-4">
      <%= f.submit "検索", class: "btn btn-outline-primary" %>
    </div>
  <% end %>

<% if user_signed_in? %>
  
  <%# 1. 自分ランキング / 全体ランキング の切り替え %>
  <div class="text-center mb-5">
    <div class="btn-group" role="group">
      <%= link_to "自分ランキング", root_path(period: @period), 
          class: "btn #{params[:tab] == 'all' ? 'btn-outline-primary' : 'btn-primary'} px-4" %>
      <%= link_to "全体ランキング", root_path(tab: "all", period: @period), 
          class: "btn #{params[:tab] == 'all' ? 'btn-primary' : 'btn-outline-primary'} px-4" %>
    </div>
  </div>

  <%# 2. チャンネルフィルター (全てのチャンネル / タワーのみ / アイランドのみ) %>
  <div class="text-center mb-5">
    <div class="btn-group btn-group-sm" role="group" aria-label="Ranking Channel">
      <% # 全てのチャンネル %>
      <%= link_to "全てのチャンネル", 
          root_path(params.to_unsafe_h.slice(:tab, :period).except(:channel)), 
          class: "btn #{params[:channel].blank? || params[:channel] == 'all' ? 'btn-info' : 'btn-outline-info'}" %>
      
      <% # タワーのみ %>
      <%= link_to "タワーのみ", 
          root_path(params.to_unsafe_h.slice(:tab, :period).merge(channel: 'tower')), 
          class: "btn #{params[:channel] == 'tower' ? 'btn-info' : 'btn-outline-info'}" %>
      
      <% # アイランドのみ %>
      <%= link_to "アイランドのみ", 
          root_path(params.to_unsafe_h.slice(:tab, :period).merge(channel: 'island')), 
          class: "btn #{params[:channel] == 'island' ? 'btn-info' : 'btn-outline-info'}" %>
    </div>
  </div>

  <%# 3. 期間フィルター (総合 / 月間 / 週間) %>
  <div class="text-center mb-5">
    <div class="btn-group btn-group-sm" role="group">
      <%= link_to "総合", root_path(tab: params[:tab], period: "all", channel: params[:channel]), 
          class: "btn #{@period == 'all' ? 'btn-dark' : 'btn-outline-dark'}" %>
      <%= link_to "月間", root_path(tab: params[:tab], period: "monthly", channel: params[:channel]), 
          class: "btn #{@period == 'monthly' ? 'btn-dark' : 'btn-outline-dark'}" %>
      <%= link_to "週間", root_path(tab: params[:tab], period: "weekly", channel: params[:channel]), 
          class: "btn #{@period == 'weekly' ? 'btn-dark' : 'btn-outline-dark'}" %>
    </div>
  </div>
<% end %>

  <% if params[:tab] == "all" %>
    <div class="card shadow-sm">
      <div class="card-body">
        <h3 class="card-title text-center mb-4 text-success">全体ランキング</h3>
        
        <% @all_rankings.each_with_index do |video, index| %>
          <% current_rank = (params[:page].to_i < 1 ? 0 : params[:page].to_i - 1) * 20 + index + 1 %>
          
          <div class="d-flex align-items-center mb-3 p-3 border rounded">
            
            <div class="me-3 text-center" style="min-width: 60px;">
              <span class="fs-3 fw-bold text-success"><%= current_rank %>位</span>
            </div>

            <div class="me-4 flex-shrink-0">
              <% if video.video_id.present? %>
                <%= link_to video_path(video) do %>
                  <img src="https://img.youtube.com/vi/<%= video.video_id %>/mqdefault.jpg" 
                       alt="<%= video.title %>のサムネイル" 
                       class="img-fluid rounded shadow-sm"
                       style="width: 160px; height: auto;">
                <% end %>
              <% else %>
                <div class="rounded bg-light text-secondary d-flex align-items-center justify-content-center border" 
                     style="width: 160px; height: 90px; font-size: 0.9em;">
                  NO IMAGE
                </div>
              <% end %>
            </div>

            <div>
                <h5 class="mb-2">
                  <%= link_to video.title, video_path(video), class: "text-decoration-none text-dark fw-bold" %>
                </h5>

                <div class="mb-2">
                  <% video.tag_list.each do |tag| %>
                    <%= link_to tag, search_index_path(tag: tag), class: "badge bg-secondary text-decoration-none me-1" %>
                  <% end %>
                </div>
                
                <span class="badge bg-success fs-6">
                  視聴回数 <%= Watch.where(video: video).sum(:watched_count) %>回
                </span>
              </div>
          </div>
        <% end %>
        
        <div class="d-flex justify-content-center mt-4">
          <%= render 'home/manual_pagination',
              total_pages: @all_total_pages,
              current_page: @current_page %>
        </div>
        
      </div>
    </div>

  <% else %>
    <div class="card shadow-sm">
      <div class="card-body">
        <h3 class="card-title text-center mb-4 text-primary">自分ランキング</h3>
        <% if user_signed_in? && @my_rankings.present? %>
          
          <% @my_rankings.each_with_index do |video, index| %>
            <% current_rank = (params[:page].to_i < 1 ? 0 : params[:page].to_i - 1) * 20 + index + 1 %>
            
            <div class="d-flex align-items-center mb-3 p-3 border rounded">
              
              <div class="me-3 text-center" style="min-width: 60px;">
                <span class="fs-3 fw-bold text-primary"><%= current_rank %>位</span>
              </div>
              
              <div class="me-4 flex-shrink-0">
                <% if video.video_id.present? %>
                  <%= link_to video_path(video) do %>
                    <img src="https://img.youtube.com/vi/<%= video.video_id %>/mqdefault.jpg" 
                         alt="<%= video.title %>のサムネイル" 
                         class="img-fluid rounded shadow-sm"
                         style="width: 160px; height: auto;">
                  <% end %>
                <% else %>
                   <div class="rounded bg-light text-secondary d-flex align-items-center justify-content-center border" 
                     style="width: 160px; height: 90px; font-size: 0.9em;">
                  NO IMAGE
                </div>
                <% end %>
              </div>

              <div>
                <h5 class="mb-2">
                  <%= link_to video.title, video_path(video), class: "text-decoration-none text-dark fw-bold" %>
                </h5>

                <div class="mb-2">
                  <% video.tag_list.each do |tag| %>
                    <%# クリックすると、そのタグで検索ページに遷移します %>
                    <%= link_to tag, search_index_path(tag: tag), class: "badge bg-secondary text-decoration-none me-1" %>
                  <% end %>
                </div>
                
                <span class="badge bg-primary fs-6">
                  視聴回数 <%= Watch.where(user: current_user, video: video).take&.watched_count.to_i %>回
                </span>
              </div>
            </div>
          <% end %>

          <div class="d-flex justify-content-center mt-4">
            <%= render 'home/manual_pagination', 
                total_pages: @my_total_pages,
                current_page: @current_page %>
          </div>

        <% elsif user_signed_in? && @my_rankings.empty? %>

          <% if current_user.api_token.present? %>
            <p class="text-center text-danger mt-4 fs-5 fw-bold">
              ⚠️ 要確認: 視聴履歴が表示されていません
            </p>
            
            <p class="text-center text-muted mt-2" style="max-width: 450px; margin: 0 auto; line-height: 1.6;">
              <span class="fw-bold">【トークン有効性チェック】</span><br>
              お手数ですが、以下の2点をご確認ください。<br>
              
              <span style="display: block; margin-top: 10px;"></span>
              
              <span style="white-space: nowrap;">
              1. マイページにて、<strong>拡張機能のトークンとマイページのAPIトークンが一致しているか</strong>。
              </span><br>
              <span style="white-space: nowrap;">
              2. トークン確認後、<strong>再度ジャルジャルタワーの動画を視聴し、履歴が反映されるか</strong>。
              </span>
            </p>
            
            <div class="text-center mt-3">
               <%= link_to "マイページへ移動", mypage_path, class: "btn btn-warning" %>
            </div>
          
          <% else %>
            <p class="text-center mt-4 fs-5 fw-bold text-primary">
              🎉 あと一歩であなた専用のランキングが見られます！
            </p>
            
            <div class="text-center mt-3 p-3 bg-light rounded border">
              <p class="mb-2">
                このランキングを表示するには、Chrome拡張機能と<strong>マイページ</strong>を連携させる必要があります。
              </p>
              <p class="mb-3 fw-bold text-muted">
                下のボタンからマイページへ進み、<strong>APIトークン</strong>をコピー＆ペーストして設定を完了してください。
              </p>
              
              <%= link_to "マイページへGO！(設定画面へ)", edit_user_registration_path, class: "btn btn-primary btn-lg" %>
            </div>
          <% end %>

        <% else %>
          <p class="text-center text-muted mt-4 fs-5">
            <strong>ログインすると、あなただけの視聴ランキングが表示されます。</strong><br>
          </p>
          
          <div class="d-flex justify-content-center mt-3">
            <%= link_to user_google_oauth2_omniauth_authorize_path, class: "btn btn-primary btn-lg me-3" do %>
              <i class="fab fa-google me-2"></i> Googleアカウントでログイン
            <% end %>
            
            <%# ゲストログイン用のボタン %>
            <%= form_with url: users_guest_sign_in_path, method: :post, local: true, class: "d-inline" do |f| %>
              <%= f.submit "ゲストとしてログイン", class: "btn btn-outline-secondary btn-lg" %>
            <% end %>
          </div>
        <% end %>

      </div>
    </div>
  <% end %>
</div>