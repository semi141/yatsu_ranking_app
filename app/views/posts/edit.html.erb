<h2>投稿編集</h2>

<%= form_with(model: @post, local: true) do |f| %>
  <div>
    <%= f.label :content, "投稿内容" %><br>
    <%= f.text_area :content %>
  </div>

  <div>
    <%= f.submit "更新する" %>
  </div>
<% end %>
