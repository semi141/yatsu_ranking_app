<h2>新規投稿</h2>

<%= form_with(model: [@video, @post], local: true, data: { turbo: false }) do |f| %>
  <div>
    <%= f.label :content, "投稿内容" %><br>
    <%= f.text_area :content %>
  </div>

  <%= f.submit "投稿する" %>
<% end %>
